{% extends 'base.html.twig' %}

{% block body %}
    <h1>ParisFlat</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ parisFlat.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ parisFlat.type }}</td>
            </tr>
            <tr>
                <th>Longitude</th>
                <td>{{ parisFlat.longitude }}</td>
            </tr>
            <tr>
                <th>Latitude</th>
                <td>{{ parisFlat.latitude }}</td>
            </tr>
            <tr>
                <th>Thumbnail</th>
                <td>{{ parisFlat.thumbnail }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ parisFlat.date }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ parisFlat.price }}</td>
            </tr>
            <tr>
                <th>Album</th>
                <td>{{ parisFlat.album }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ parisFlat.description }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('parisflat_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('parisflat_edit', { 'id': parisFlat.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
